#include "Dog.h"
#include "Cat.h"
#include "Bird.h"
#include <iostream>

int main() {
    // Array of Animal pointers
    // TODO: Define a constant for the number of animals
    // TODO: Create an array of Animal pointers with the defined size

    // Adding different animals
    // TODO: Allocate new Dog, Cat, and Bird objects and assign them to the array

    // Polymorphic makeSound calls
    // TODO: Iterate through the array and call makeSound on each animal

    // Clean up
    // TODO: Iterate through the array and delete each animal to avoid memory leaks

    return 0;
}
